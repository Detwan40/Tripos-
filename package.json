{
  "name": "tripos-travel-booking",
  "version": "1.0.0",
  "description": "Nomad travel agency booking platform - Book flights, hotels, activities, and cars in one place",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "keywords": [
    "travel",
    "booking",
    "flights",
    "hotels",
    "activities",
    "cars",
    "nomad",
    "vacation"
  ],
  "author": "Detwan40",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
